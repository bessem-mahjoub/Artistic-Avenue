<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Example 1</title>
    <link rel="stylesheet" href="{{asset('style.css')}}" media="all" />
</head>
<body>


<header class="clearfix">
    <div id="company" class="clearfix"></div>
    <h1 class="align-center table-title"><div>Liste des commandes</div></h1>

</header>

<main>
    <table class="table">
        <thead>
            <tr>
                <th class="table-header">Id</th>
                <th class="table-header">Nombre Commande</th>
                <th class="table-header">Nom de Produit</th>
                <th class="table-header">Localisation</th>
                <th class="table-header">Date</th>
            </tr>
        </thead>
        <tbody>
            {% for commande in commandes %}
            <tr>
                <td class="table-cell">{{ commande.id }}</td>
                <td class="table-cell">{{ commande.nombreP }}</td>
                <td class="table-cell">{{ commande.produit }}</td>
                <td class="table-cell">{{ commande.localisation }}</td>
                <td class="table-cell">{{ commande.date ? commande.date|date('Y-m-d') : '' }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</main>

</body>
<style>
    body {
        font:normal 12px/20PX "Liberation sans", Arial, Helvetica, sans-serif;
        color:#585858;
    }
    img {
        width:200px;
        margin:200px;
    }
    .table {
        border-collapse: collapse;
        width: 100%;
        max-width: 800px;
        margin: auto;
        background: #f7f7f7;
    }
    .table-header {
        background: #333;
        color: #fff;
        font-weight: bold;
        text-align: left;
        padding: 10px;
    }
    .table-cell {
        border: 1px solid #ccc;
        padding: 10px;
        text-align: left;
    }
    .table-title {
  text-align: center;
  font-weight: bold;
  background-color: #333;
  color: #fff;
  padding: 10px;
}

</style>
</html>
