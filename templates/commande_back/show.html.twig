{% extends 'baseback.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}
    <h1>Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>NombreP</th>
                <td>{{ commande.nombreP }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commande_back_index') }}">back to list</a>

    <a href="{{ path('app_commande_back_edit', {'id': commande.id}) }}">edit</a>

    {{ include('commande_back/_delete_form.html.twig') }}
{% endblock %}
