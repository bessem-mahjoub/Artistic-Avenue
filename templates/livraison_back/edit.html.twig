{% extends 'baseback.html.twig' %}

{% block title %}Edit Livraison{% endblock %}

{% block body %}
    <h1>Edit Livraison</h1>

    {{ include('livraison_back/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_livraison_back_index') }}">back to list</a>

    {{ include('livraison_back/_delete_form.html.twig') }}
{% endblock %}
