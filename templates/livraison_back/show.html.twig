{% extends 'baseback.html.twig' %}

{% block title %}Livraison{% endblock %}

{% block body %}
    <h1>Livraison</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdLiv</th>
                <td>{{ livraison.idLiv }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ livraison.adresse }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ livraison.tel }}</td>
            </tr>
            <tr>
                <th>PtRl</th>
                <td>{{ livraison.ptRl }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_livraison_back_index') }}">back to list</a>

    <a href="{{ path('app_livraison_back_edit', {'idLiv': livraison.idLiv}) }}">edit</a>

    {{ include('livraison_back/_delete_form.html.twig') }}
{% endblock %}
