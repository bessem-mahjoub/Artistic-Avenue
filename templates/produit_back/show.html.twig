{% extends 'baseback.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}
    <h1>Produit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit.id }}</td>
            </tr>
            <tr>
                <th>NomProd</th>
                <td>{{ produit.nomProd }}</td>
            </tr>
            <tr>
                <th>NbrProd</th>
                <td>{{ produit.nbrProd }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ produit.categorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_produit_back_index') }}">back to list</a>

    <a href="{{ path('app_produit_back_edit', {'id': produit.id}) }}">edit</a>

    {{ include('produit_back/_delete_form.html.twig') }}
{% endblock %}
